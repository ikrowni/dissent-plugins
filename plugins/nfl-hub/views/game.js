// views/game.js — placeholder. Replaced with the real implementation in a later task
// of wave 1B; exists now only so the app shell's module graph resolves.
import { stateMsg } from '../core/ui.js';

export function render() { return stateMsg('Not implemented yet.'); }
