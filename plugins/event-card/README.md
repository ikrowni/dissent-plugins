# Event Card

Display a live event card — fight cards, match schedules, esports brackets.

## Configuration

- `api_url` — JSON API URL (required)
- `event_title` — Event Title Override

## Permissions

No special permissions required.
